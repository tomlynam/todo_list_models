<h1>Editing List: <%= @list.name %></h1>
<br>

<%= form_for @list do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :description %>
	<%= f.text_field :description %>	

	<%= f.submit %>
<% end %>